export function OpsRoute() {
  return (
    <div class="page pad">
      <div class="card wide">
        <h1 class="h1">Ops</h1>
        <p class="muted">Optional route (flag + role-gated). Not enabled by default.</p>
      </div>
    </div>
  );
}
